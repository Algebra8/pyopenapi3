Customer:
  description: A SeeTickets customer
  properties:
    email:
      example: some_user@gmail.com
      format: email
      readOnly: true
      type: string
    firstName:
      example: Mike
      readOnly: true
      type: string
    id:
      format: int64
      readOnly: true
      type: integer
    lastName:
      example: Cat
      readOnly: true
      type: string
  type: object
Store:
  description: A store for buying things
  properties:
    customer:
      $ref: '#/components/schemas/Customer'
    id:
      format: int64
      readOnly: true
      type: integer
  type: object
